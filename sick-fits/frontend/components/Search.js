import { SearchStyles } from './styles/DropDown';

const Search = () => {
  const search = 'Search...';
  return (
    <SearchStyles>
      <p>{search}</p>
    </SearchStyles>
  );
};

export default Search;
